// -*- mode: C++ -*-
/*
 *  Copyright (c) `(format-time-string "%Y")`, GITAI Inc.
 *  All rights reserved.
 * `(file-name-nondirectory (buffer-file-name))`
 * Author: `(user-full-name)` <`(user-mail-address)`>
 */


#ifndef `(upcase (replace-regexp-in-string "[^a-zA-Z0-9]" "_" (format "%s_%s" (file-name-nondirectory (directory-file-name (file-name-directory (buffer-file-name)))) (file-name-nondirectory (buffer-file-name)))))`
#define `(upcase (replace-regexp-in-string "[^a-zA-Z0-9]" "_" (format "%s_%s" (file-name-nondirectory (directory-file-name (file-name-directory (buffer-file-name)))) (file-name-nondirectory (buffer-file-name)))))`

$0

#endif // `(upcase (replace-regexp-in-string "[^a-zA-Z0-9]" "_" (format "%s_%s" (file-name-nondirectory (directory-file-name (file-name-directory (buffer-file-name)))) (file-name-nondirectory (buffer-file-name)))))`